# 工作纪律 · v3.0 Pipeline

## 强制规则 (违反=偷懒)

### 🚨 推送前汉化检查 (最高优先级 · 违反=自残)

**任何 git push 之前必须跑这条命令，不通过不准 push：**

```bash
python pre_push_check.py
```

检查项：
- HTML/JSON 报告的可读文本是否含英文（CSS/标签除外）
- commit message 是否中文
- 归档链接描述是否中文
- 推送文件名是否中文命名

**不通过 → 修改后重新检查 → 通过才 push。没有例外。**

### 🧭 市场参考防带偏纪律 (2026-08-13 确立)

**核心原则: 方向信市场，结构信模型，交叉地带信谁都不行→跳过。**

1. **模型先出数，市场后入场** — 模型先用ELO+DC独立算概率，市场只在贝叶斯融合一步进入。冷启动(市场即模型)场次永远不许下注。
2. **看移动，不看水位** — 赔率水位含抽水扭曲；赔率移动才是信息。静止赔率只当方向锚。
3. **翻市场需要双重证据** — 模型翻市场方向必须同时满足: 置信≥60% + 结构性理由(ELO差/联赛风格/深盘规则)。当前分歧胜率2-2，模型尚无翻市场资格。
4. **绝不逆势下注** — 赔率朝某方向移动=信息在流动。只在移动停止后的稳定期下注，或顺移动确认方向下注。
5. **两个都犹豫就跳过** — 模型平局>25% + 市场在移动 + 冷启动，信号叠满→跳过。跳过不丢人，硬猜才丢人。

**体彩赔率角色定位:**
- 方向锚 ✅ 可信 | 信息源(移动) ✅ 可看 | λ反推器 ✅ 可用(结果不参与投注) | edge来源 ❌ 不可能
- 用体彩赔率打败体彩逻辑不成立(13%抽水+国际镜像)。edge只来自市场不看的地方: 大小球/平局修正/联赛结构。

### 每次生成预测后必须:

1. ✅ 更新 `archive.html` — 在对应日期下追加预测和复盘链接
2. ✅ `python pre_push_check.py` ← 🚨 推送前汉化检查（不通过不push）
3. ✅ `git add -A && git commit -m "中文说明" && git push origin master`
4. ✅ 确认 push 成功才结束任务

### 每次复盘必须:

1. ✅ 如实报告命中率，不编造不美化
2. ✅ 数字一字不改 — Brier/Accuracy/ROI 从 review 命令输出直接抄
3. ✅ ELO 更新确认：review 命令自动更新，验证 state 文件已保存
4. ✅ **多维度复盘** — review 命令自动输出5维度(1X2/OU25/OU35/BTTS/AH)成绩，累计到 `data/state/dimension_ledger.json`，推送时报告各维度状态

### 维度门禁 (样本≥30场/维度后启用)

每个维度是独立预测任务，必须**模型Brier < 气候基线Brier**(比恒猜多数派更有信息量)才允许产生投注信号：
- 1X2 胜平负 — 当前41场: 0.2185 vs 0.24 ✅微弱技能
- OU25 大小球2.5 — 当前41场: 0.2626 vs 0.24 ❌无技能
- OU35 大小球3.5 — 当前41场: 0.1871 vs 0.1956 ✅微弱技能
- BTTS 双方进球 — 当前41场: 0.2678 vs 0.24 ❌无技能
- AH 让球盘 — 0样本，从8月12日开始累计
- 未过门维度禁止下注；过门维度按edge>5%+Kelly>1%+非冷启动执行

### 每次预测必须包含:

1. ✅ 模型输出: Dixon-Coles 胜平负概率 + 贝叶斯后验 + Kelly 值
2. ✅ 市场对比: 模型 vs 市场 edge 检测
3. ✅ 冷启动标记: 数据不足的场次标注 COLD START

## v3.0 每日操作

```bash
# 1. 获取今日比赛 (JSON格式，含赔率)
python pipeline.py predict --matches-json data/today.json

# 2. 查看预测报告
# data/output/predictions_YYYY-MM-DD.html

# 3. 比赛结束后 — 复盘
python pipeline.py review YYYY-MM-DD --results-text "TeamA 2-1 TeamB\n..."
# 或: echo '[{"home_team":"A","away_team":"B","home_goals":2,"away_goals":1}]' > data/output/results_DATE.json
# python pipeline.py review YYYY-MM-DD

# 4. 查看复盘报告
# data/output/review_YYYY-MM-DD.html
```

## 模型门禁 (硬性)

- 回测 Brier < 基线 Brier (0.65) → PASS ✅ (当前 0.60)
- 回测未通过 → 禁止上线，修复模型后重试
- `python pipeline.py backtest` 验证

## 深盘规则 (v2.0 教训，v3.0 继承)

v2.0 中 5场让球>1.0深盘仅1场穿盘，经验保留：
- ELO差>200 + 联赛场均进球<2.5 → 深盘(>1.5)自动降一档
- 参考联赛风格: 克罗地亚/波兰/捷克 → 天然小球
- 赔率以实时数据为准，不依赖搜索缓存

## 克劳德独立分析师 (Phase 4)

`--llm` flag 启用后:
- Claude 收到结构化证据包 (ELO, DC概率, 联赛画像)
- 只做定性评估: 伤停/战意/杯赛阶段等模型盲区
- 不碰数字，不生成概率
- 标注 "分析师注释(LLM)" 与模型输出明确区分
- API不可用时预测正常运行

## 技术债务

- ~~scipy MLE~~ ✅ 已装 (scipy 1.18.0, L-BFGS-B + 解析梯度, 365次迭代收敛, Brier改善~0.002-0.007)
- ~~近期状态因子~~ ✅ Form factor + 时序形态 + 指数加权, Brier改善~0.005
- ~~动态市场权重~~ ✅ Pinnacle margin驱动贝叶斯M权重扩大到[5,45]
- ~~冷启动改进~~ ✅ 联赛中位数att/def替代盲猜1.0, Brier改善~0.003 (fold 25-26最大)
- 综合Brier: 0.6259 (baseline 0.651), 较分析基线(0.638)改善0.012
- Phase 4 LLM 集成待实现
- odds-api.io 实时赔率拉取有时 GBK 编码问题 (Windows终端)
- 下一步可探索: 伤停/战意信号、联赛升级降级追踪、深度盘规则
