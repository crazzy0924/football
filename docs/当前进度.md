# 当前进度 · 交接文件

> 用途: **会话上下文会被自动压缩, 聊天里的细节会丢。所以只认文件, 不认记忆。**
> 任何新会话/被压缩后的自己, 开工先读这份。
>
> 维护约定: 每完成一件在办事项就更新这里的「在办」和「最近完成」。

最近更新: 2026-09-24

## 一、在办

### 🆕 欧国联预测子系统（2026-09-24 新建 · 当前最活跃）

**用户拍板**: 只做 League A (16 队) / 另起独立国家队模型 / **MD1 就要出预测** / 纪律全部沿用。

- 入口: `python national/run_nl.py --date <体彩比赛日>` —— **手动触发，未接排程/推送/复盘/自检**
- 完整方案 / 验证 / 已知问题: `docs/欧国联预测方案.md`
- 已建: 独立语料(49,547 场国际比赛) + DC(支持中立场) + 国家队 Elo + 对数意见池 + 留出验证
- **验证(2026-09-24 修正后，这条最重要)**: 用"欧足联内部+正式"当**代理口径**时 Brier 0.45488 / 66.9% (n=124)；
  但换成**真正的目标口径（League A 强强对话，n=102）技能无法证明** —— 相对无技能基准只改善 **1.9%**，
  对 Elo t=−1.19 **不显著**。专项滚动回测: `national/backtest_nl.py`；跨赛事: `national/backtest_elite.py`。
- **样本量现实**: 要检出 Brier 差 0.02 需 **933 场**（0.01 需 3732 场）；16 强之间 2018 年至今只有 270 场，
  欧国联每年 +48 场 → **靠自己的数据永远证明不了「模型 vs 市场」**，`w` 只能是先验保守选择，调不出来。
- **未证明**: 与市场谁强 —— 没有历史国家队赔率（Kaggle 需登录 / GitHub raw 连接重置 / Apify 收费）。
- **挪威问题已查清 (2026-09-24)**: 病灶是 **DC 单模型**（我们自己的 Elo 站市场那边: 挪威 Elo #13 vs DC #16），
  根因是 DC 无动量、跟不上快速崛起的队。**但 4 种修法全部无效**（半衰期/校准/掺Elo/分歧时换Elo），
  基线: 分歧时 DC 并不输 Elo。→ **分歧预警的正确用法是去查场外信息，不是改模型**。
  复现: `python national/compare_dc_elo.py`。详见方案文档第 9.1 节。
- **四个特征全部判决完毕 (2026-09-24)**: 休息天数 **不加**（窗口内 t=−0.07，系数 −0.0008 球/天）；
  阵容连续性 **做不了**（SofaScore 403 / api-football 429 / football-data.org 无欧国联 / TheSportsDB 日限 3 场）；
  战意分层 **只做展示**（有分化的历史场次约 60 场，检出 0.02 效应需 933 场，不可验证）→ `national/standings.py`。
  中立场信息仍无来源。详见方案文档第 9.2 节。
- **方案A 已选定并落地 (2026-09-24)**: 模型定位 = **诊断而非预测**。
  `w=0.10` + 分歧预警(≥8个点打⚠) + 赔率存档(`data/national/odds_log.jsonl`) + 语料刷新(`--refresh`)。
- **MD1 实际范围**: 体彩挂了 **12 场欧国联**, League A 命中 **6 场** (9/25 02:45 四场 + 9/26 02:45 两场)，
  其余 6 场是 B/C 级已排除。**3 场触发分歧预警**: 荷兰-德国 +10.1 / 挪威-丹麦 −14.3 / 意大利-比利时 +8.5。

### 🎯 八维盲判（2026-09-24 新建 · **用户要求"要有自己的预测方式"，这是当前主线**）

**核心: 先形成判断，再看价格。** 而不是先看价格再解释价格。

- 入口 `python national/octa.py --date <比赛日> --repeats 3`；自检 `national/check_calls.py`
- 打分 `national/score_calls.py` —— **主判据 = 只考核「反向」场次**（同向只是在附和市场，没有信息量）
- 完整方法论 → `docs/八维盲判方法论.md`
- 三段: ①遮赔率做八维(市场权重0) ②揭开赔率, 默认立场"我的判断成立", 只有"市场揭示了我确实漏掉的具体事实"才能改判
  ③落账(`data/national/calls_ledger.jsonl`, 只追加, 靠 revision 去重)
- **实测修掉三个真问题**: ①LLM 自报方向分 **6 场错 4 场**(一场符号都反了) → 计算权收回程序；
  ②同一份证据跑 5 次出过 **3 种结论** → 自洽性投票 K=3 + 温度 0.35 → 修后 **5/6 场 3/3 一致**；
  ③优势分正负号定义不清 → 写死并给例。自检 C1–C6 现已 **0 条硬问题**。
- **MD1 六场已落账**: **反向 2 场**（塞尔维亚vs希腊 客胜0-1 / 意大利vs比利时 平局1-1），同向 4 场。
- **对照轨**: `run_nl.py`（DC + w=0.10 市场主导）保留，两轨都落账、一起打分。
- ⚠️ **无法回测**（情报是当天抓的，回溯不了 2018），只能前向攒样本；**显著性永远拿不到**（48 场/年）。
- ⚠️ **情报靠主代理 web_search 抓**（`data/national/intel_<日期>.txt`），不是爬虫 ——
  因为 `cn.bing.com` 通道已失效（两个不同查询返回完全相同结果），`pipeline/injury_recon.py` 很可能一直是空的。**待查**。

### 💣 已修: 俱乐部主链路的情报被静默清空 (2026-09-24 发现并修复)

- **范围**: 五大联赛+欧冠那条链路 (`daily_run.py` → `injury_recon.py` → `intel_fetcher.py`)
- **Bug**: `intel_fetcher.py` 用截断模式**无条件**写 `data/intel/<日期>.txt`，
  `if intel_text:` 是**打印守卫不是写入守卫**（写在 write 之后）。采集不到时把主代理
  当天写好的整份情报清成 2 字节，然后才打印"未采集到"。
- **实测影响**: 2026-09-04 起连续约 20 天，比赛日的情报文件全是 2 字节 →
  八维里「阵容伤停14 + 天气场地6 + 裁判6 = 26%」的权重一直在无证据状态下打分。
- **已修**: `intel_fetcher.py` + `intel_fetcher_fpl.py`（写入前判空 + 保留已有非本模块内容），
  猴子补丁验证通过（87 字节 → 87 字节，手写情报保住）。
- **已机制化**: 新增 self_check **B6「情报文件未被清空」**；CLAUDE.md 立「写入守卫≠打印守卫」条。
- **追根**: `cn.bing.com` 通道也失效了（两个不同查询返回完全相同结果）→
  `injury_recon.py` 其实也一直是空转。**这两件事叠加**才是"情报长期为空"的完整原因。

### ⏰ 今晚 21:00 要做的事（2026-09-24 用户要求）

**用户原话**: 「今晚9点再重新出一份吧，毕竟还太早，水位跟临场信号估计还会有补充」

**背景**: 15:26 出的第一版判断已落账 (revision 1)。但那时**部分场次情报是空的**
（葡萄牙vs威尔士、荷兰vs德国基本没抓到）。临场前会有：水位变动 + 名单/伤停补充 + 天气裁判。

**关键认知**: 盲判是**遮赔率**的，所以单纯重跑不会产生新信息 ——
21:00 那份的价值**全在「新情报 + 新水位」**。而情报必须由主代理 web_search 抓，
**脚本抓不了**，所以这件事不能做成纯定时任务。

**流程 (21:00)**：
1. 主代理重新 `web_search` 抓六场情报 → 更新 `data/national/intel_2026-09-24.txt`（覆盖，标注第二版时间）
2. 赔率/语料已由后台任务自动刷新（见下）
3. `python national/octa.py --date 2026-09-24 --repeats 3` → **revision 2**
4. `python national/check_calls.py --date 2026-09-24` 自检
5. `python national/make_page.py --date 2026-09-24` → 重出判断页
6. 对比 revision 1 vs 2：**是否改判、为什么改**（用户要看到这个）

**已挂后台任务**: 21:00 自动跑 `refresh_corpus.py` + `run_nl.py`（刷新语料与赔率快照）。
跑完会通知主代理，由主代理接着做情报与重判。**若任务被会话重启杀掉，手动补跑这条即可。**

### xG 评估（已结案 2026-09-15，保留备查）

**结论: 不接入生产。**

- 抓取**已完成**: 30 个文件 / 8387 场 / 含 xG 7913（94.3%），1.3 MB
  落在 `data/state/xg/<联赛>_<赛季>.json`（该目录 gitignore，不入库）
- **配对检验结论: xG 有效**。w=0.5 时逐场 Brier 差 -0.00160（t=-3.94, n=5402），
  三个完整赛季**各自独立显著**，四档权重单调递增 → 详见 `docs/xG接入评估计划.md` 第七节
- **生产链路复验已完成**: 融合后增益从 -0.00160 缩到 **-0.00070**（t=-2.72），
  四个赛季**无一单独显著**，只填平了与市场差距（0.0111）的 **6.3%**
- **结论: 建议暂不接入生产**。理由见 `docs/xG接入评估计划.md` 第八节
  （增益过小 / 证据脆弱 / 会引入"每轮续抓 xG"的持续运维依赖）
- **试跑已做 (2026-09-14 终盘)**: 开 `FOOTBALL_XG_WEIGHT=0.5` 重训后跑了一次今天的终盘,
  与正常模型对比 —— 5 场里最大只挪 2.3pp, **方向一个都没变**。与"增益 0.0007"的证据吻合。
  模型已恢复原样, **未采纳**。
- **xG 现状**: 代码已就位（`pipeline/xg_blend.py` + trainer 接线, 默认关闭）,
  想永久开启就设环境变量 `FOOTBALL_XG_WEIGHT=0.5`（可选 `FOOTBALL_XG_LEAGUES=BL1` 只给 BL1）。
- **已结案 (2026-09-15)**: 维持不接入。代码保留、默认关闭。
- **结论已归档**: `docs/xG接入评估计划.md` 第七、八节

## 一之二、结构性欠账（2026-09-15 已处理两项）

**欠账 A · 队名打通靠人工补丁** —— 同类故障复现 4 次（Rennes / Brest / 勒芒 / Parma-Inter）。
已改成机制：`tools/check_unmatched.py` 把两侧对不上的队名写进
`data/state/unmatched_names.json`，并通过自检 **F1 项**每天自动报。
只盯"预测侧结算不上"的（真缺口），不报赛果源的英冠孤儿（预期噪音）。

**欠账 B · 固定钟点与跨时区赛程错位** —— 终盘挂 21:00 但首场可能 20:00 就开踢；
09-15 早盘把凌晨已结束的三场又预测了一遍。已改成：预测入口**跳过已开赛场次**
（凌晨场次归属用"离现在最近的时间解释"消歧）。

## 工作时段（2026-09-15 用户重新捋定）

| 时点 | 阶段 | 做什么 |
|---|---|---|
| **07:00** | 复盘 | 复盘昨日；**不只对结果，要查漏补缺、归因错误、查是否执行规则/偷懒** |
| **08:00** | 早盘 | 拉体彩当天"比赛日"场次（五大+欧冠），出八维早盘分析 |
| **16:00** | 午盘 | 午盘分析 |
| **规则算** | 终盘 | 见下方"终盘时刻规则"，出预测并冻结（哈希链） |
| 周一 06:00 | 重训 | 刷 CSV + 全量重训 + 回测 + 周复盘 |

- 深夜（00:00 后）场次：用户多数会**主动叫我**临时出终盘
- 预测入口两道保护：跳过已开赛场次 + 近 3 天有赛果的场次一律剔除

### 终盘时刻规则（2026-09-19 用户拍板 · 写死）

    默认                        21:00
    当日有早于 21:00 开赛的场次  →  20:00
    且 不晚于"最早开赛前 30 分钟"   ← 下限 (B 案)

由 tools/schedule_final.py 每天在早盘/午盘后各算一次并写入计划任务；
**同时把 --date <比赛日> 钉进任务参数**（终盘常在午夜后跑，不钉会拉错一天）。
改完**当场读回 NextRunTime 校验**，不对就报警退出码 3。

### 这套时段还缺两块（待办）

1. **临盘阶段未定义**：用户要"出终盘之前做一份临盘分析给出预测"。
   现在终盘(20:00)就是出预测的阶段。需要定：临盘是独立阶段还是终盘改名；
   若独立，时点跟"当天最早开赛前 N 小时"走（体彩当天场次最早可能 18:00 开踢）。
2. **复盘归因未自动化**：现在复盘命令只做"对结果"，
   "归因错误 + 查规则/偷懒"仍靠人工。要做成固定流程须先定归因写到哪里、谁来读。

## 一之三、收敛后的在办清单

| 项 | 状态 |
|---|---|
| xG 接入 | **结案**（不接入，代码默认关闭） |
| 队名机制化 | **完成**（F1 每日报） |
| 跳过已开赛 | **完成** |
| 终盘时点 21:00 → 20:00 | **完成**（深夜场次由用户临时叫） |
| 赛季重放工具 | 已完成（`tools/replay_season.py`） |
| PSI 漂移预警 | 已完成（等赛季推下去才有样本） |

### 查当前状态（照抄即可）

    cd D:\足球大模型1.0
    python -c "import json,glob;print(sum(len(json.load(open(f,encoding='utf-8'))['matches']) for f in glob.glob('data/state/xg/*.json')))"

### 判断抓完了没

每个联赛-赛季文件里的场次数达到下列量级即为完整（欧冠含资格赛会多几十场）:

    PL 380 / PD 380 / BL1 306 / SA 380 / FL1 306 / UCL 200+

## 二、下一步（按顺序，不要跳步）

详细口径见 `docs/xG接入评估计划.md`，这里只列动作。

1. **造特征**: 构造严格**赛前**的累计 xG / xGA（先取特征再加入累加器，防泄漏）
2. **对齐语料**: 用 `pipeline/team_names.py` 的规范名把 xG 表与 `data/historical_odds` 对接，
   **必须核对未对齐比例**，否则样本会静默变少
3. **配对检验**: xG 版 DC vs 现版 DC，在完全相同的比赛上比逐场 Brier 差
   - 判定: t 值绝对值 > 2 才算成立，否则如实报「不显著，不采用」
   - 必须分联赛单独看（时间衰减的教训: PL 显著改善、FL1 显著变差、总体互相抵消）
   - 必须同时输出市场去水概率的 Brier 作参照线

## 三、最近完成（倒序）

1. **xG 数据源调查** —— 结论: 复用 SofaScore，不用 StatsBomb 开放数据（零当季）
   存档于 CLAUDE.md 第十一节
2. **xG 抓取器** —— `pipeline/xg_fetcher.py`，断点续抓，已验证通道
3. **赛季重放工具** —— `tools/replay_season.py`，用快照重建输入跑生产入口
   干净口径命中率 53.4%（无泄漏）/ 57.2%（生产口径，含前视泄漏，勿引用）
4. **PSI 漂移预警** —— `pipeline/drift_monitor.py`，含小样本闸门与共同球队口径
5. **时间衰减** —— 配对检验后**仅 PL 采用 λ=0.003**，其余联赛保持 0
6. **迭代框架修订版** —— `docs/量化迭代框架_修订版.md`

## 四、长期纪律（别在压缩中丢掉的东西）

- **不输出任何下注建议**。系统定位是分析展示平台。样本外 1752 场与实盘 38 场已证明
  下注信号是反向指标。见 CLAUDE.md 第九节。
- **判断模型改动一律用配对检验**（逐场 Brier 差的 t 值），不比 Brier 绝对值。
- **跑赢常数基准不等于跑赢市场**。回测必须同时给出市场参照线。
- **分联赛定值，禁止一套模板套所有联赛。**
- 说时间前先跑 `Get-Date`。
