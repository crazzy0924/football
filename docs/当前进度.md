# 当前进度 · 交接文件

> 用途: **会话上下文会被自动压缩, 聊天里的细节会丢。所以只认文件, 不认记忆。**
> 任何新会话/被压缩后的自己, 开工先读这份。
>
> 维护约定: 每完成一件在办事项就更新这里的「在办」和「最近完成」。

最近更新: 2026-09-14

## 一、在办

**xG 评估已完成，下一步是决定要不要接进生产。**

- 抓取**已完成**: 30 个文件 / 8387 场 / 含 xG 7913（94.3%），1.3 MB
  落在 `data/state/xg/<联赛>_<赛季>.json`（该目录 gitignore，不入库）
- **配对检验结论: xG 有效**。w=0.5 时逐场 Brier 差 -0.00160（t=-3.94, n=5402），
  三个完整赛季**各自独立显著**，四档权重单调递增 → 详见 `docs/xG接入评估计划.md` 第七节
- **生产链路复验已完成**: 融合后增益从 -0.00160 缩到 **-0.00070**（t=-2.72），
  四个赛季**无一单独显著**，只填平了与市场差距（0.0111）的 **6.3%**
- **结论: 建议暂不接入生产**。理由见 `docs/xG接入评估计划.md` 第八节
  （增益过小 / 证据脆弱 / 会引入"每轮续抓 xG"的持续运维依赖）
- **试跑已做 (2026-09-14 终盘)**: 开 `FOOTBALL_XG_WEIGHT=0.5` 重训后跑了一次今天的终盘,
  与正常模型对比 —— 5 场里最大只挪 2.3pp, **方向一个都没变**。与"增益 0.0007"的证据吻合。
  模型已恢复原样, **未采纳**。
- **xG 现状**: 代码已就位（`pipeline/xg_blend.py` + trainer 接线, 默认关闭）,
  想永久开启就设环境变量 `FOOTBALL_XG_WEIGHT=0.5`（可选 `FOOTBALL_XG_LEAGUES=BL1` 只给 BL1）。
- **待你拍板**: 维持不接入（推荐）/ 只接 BL1 / 全局接入

### 查当前状态（照抄即可）

    cd D:\足球大模型1.0
    python -c "import json,glob;print(sum(len(json.load(open(f,encoding='utf-8'))['matches']) for f in glob.glob('data/state/xg/*.json')))"

### 判断抓完了没

每个联赛-赛季文件里的场次数达到下列量级即为完整（欧冠含资格赛会多几十场）:

    PL 380 / PD 380 / BL1 306 / SA 380 / FL1 306 / UCL 200+

## 二、下一步（按顺序，不要跳步）

详细口径见 `docs/xG接入评估计划.md`，这里只列动作。

1. **造特征**: 构造严格**赛前**的累计 xG / xGA（先取特征再加入累加器，防泄漏）
2. **对齐语料**: 用 `pipeline/team_names.py` 的规范名把 xG 表与 `data/historical_odds` 对接，
   **必须核对未对齐比例**，否则样本会静默变少
3. **配对检验**: xG 版 DC vs 现版 DC，在完全相同的比赛上比逐场 Brier 差
   - 判定: t 值绝对值 > 2 才算成立，否则如实报「不显著，不采用」
   - 必须分联赛单独看（时间衰减的教训: PL 显著改善、FL1 显著变差、总体互相抵消）
   - 必须同时输出市场去水概率的 Brier 作参照线

## 三、最近完成（倒序）

1. **xG 数据源调查** —— 结论: 复用 SofaScore，不用 StatsBomb 开放数据（零当季）
   存档于 CLAUDE.md 第十一节
2. **xG 抓取器** —— `pipeline/xg_fetcher.py`，断点续抓，已验证通道
3. **赛季重放工具** —— `tools/replay_season.py`，用快照重建输入跑生产入口
   干净口径命中率 53.4%（无泄漏）/ 57.2%（生产口径，含前视泄漏，勿引用）
4. **PSI 漂移预警** —— `pipeline/drift_monitor.py`，含小样本闸门与共同球队口径
5. **时间衰减** —— 配对检验后**仅 PL 采用 λ=0.003**，其余联赛保持 0
6. **迭代框架修订版** —— `docs/量化迭代框架_修订版.md`

## 四、长期纪律（别在压缩中丢掉的东西）

- **不输出任何下注建议**。系统定位是分析展示平台。样本外 1752 场与实盘 38 场已证明
  下注信号是反向指标。见 CLAUDE.md 第九节。
- **判断模型改动一律用配对检验**（逐场 Brier 差的 t 值），不比 Brier 绝对值。
- **跑赢常数基准不等于跑赢市场**。回测必须同时给出市场参照线。
- **分联赛定值，禁止一套模板套所有联赛。**
- 说时间前先跑 `Get-Date`。
