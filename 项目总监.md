# 项目总监 · 整合总控 (v1.0)

> 本文件是「足球大模型」整合后的唯一总控文档。董事长定方向，项目总监执行，sky4.0 量化组出数，克劳德独立交叉，最终只产出一份「我的预测」。

---

## 一、角色定位

| 角色 | 是谁 | 职责 | 产出 |
|---|---|---|---|
| **董事长** | 用户本人 | 定方向、拍板大决策、确认新教训入库 | 指令 |
| **项目总监** | 本会话（整合后唯一入口） | 编排全流程、同路/分歧终审、对账、回写经验库 | 唯一「我的预测」+ 流水线 |
| **sky4.0 量化组** | `sky日记/`（组长 + 子代理） | Dixon-Coles + ELO + 贝叶斯 + 凯利，出量化概率与看好方向 | `predictions_YYYY-MM-DD.json/html` + 八维笔记 |
| **克劳德交叉分析师** | 独立定性核验（DeepSeek 通道，可人工加研） | 抓 sky4.0 的 P0/P1/P2 反向错误、做同路/分歧判定 | 每场「同意/保留/反对 + 方向 + 问题等级」 |

**整合原则**：量化（sky4.0）先出数 → 定性（克劳德）独立交叉 → 项目总监合并 → 一份预测。**同路命中率更高，分歧场不硬跟。**

---

## 二、系统全景（桥接架构 · 2026-09-09 定）

> **两个物理位置，一条桥接链路**：sky4.0 量化组的「活系统」在 D 盘，克劳德交叉侧在 E 盘。整合流水线**从 D 盘读活产出，写到 E 盘**，不碰活系统。

```
D:\足球大模型1.0\              ← sky4.0 量化组【活系统】(计划任务指向这里, 有 DEEPSEEK key)
├── pipeline.py / daily_run.py       量化主入口 (09:00/10:00/18:00/21:00 计划任务)
├── pipeline/analyst.py              八维 LLM 分析师 (DeepSeek)
├── config.py + .env                 含 DEEPSEEK_API_KEY / ANTHROPIC_API_KEY
└── data/output/                     预测/复盘/八维笔记产出 (gitignore, 不跨目录同步)

E:\足球大模型\                  ← 项目总监 + 克劳德交叉侧 (本会话工作目录)
├── 项目总监.md                      本文件 (唯一总控入口)
├── 整合流水线.py                    一键全流程 (桥接: 读 D盘 → 交叉 → 写 E盘)
├── 整合流水线.cmd                   22:10 计划任务入口
├── sky日记/
│   ├── pipeline/claude_cross.py     克劳德独立交叉核验模块 (本整合新增)
│   └── data/output/claude_cross_*.json  交叉结果 (写到 E盘, 不写活系统)
├── 我的预测/                        整合汇总「我的预测」产出 (推送走 D盘 git, push_predictions.ps1 已弃用)
│   ├── latest.md                    最终预测源文件
│   ├── render_predictions.ps1       md → html (文件名从标题提取日期)
│   └── html/                        汇总页 (predictions_*.html / latest.html / index.html)
└── ajunai-football/                 八维研究框架 + 独立审计技能 (克劳德方法论来源)
```

**桥接规则**：`整合流水线.py` 用 `active_sky()` 自动定位活系统——优先 `D:\足球大模型1.0`，不存在才回退 `E:\sky日记`。克劳德交叉结果落在 E 盘；最终汇总 HTML 由 `整合流水线.py` 直接写到 D 盘 sky 路径 `data/output/predictions_*.html` 并从 D 盘推 GitHub。`my_preds/` 已停更，仅作历史存档。

---

## 三、全流程流水线（整合后 · 8 步）

| 步 | 动作 | 执行者 | 产物 |
|---|---|---|---|
| 1 | 拉取当日赔率/赛程/伤停/积分榜 | sky4.0（`daily_run.py predict`） | `data/today.json` + 情报 |
| 2 | 量化预测 + 八维 LLM 分析 | sky4.0（`pipeline.py predict --llm`） | `predictions_*.json` + `analysis_notes_final_*.json` |
| 3 | 克劳德独立交叉核验（每场：同意/保留/反对 + 方向 + P0/P1/P2） | 克劳德（`claude_cross.py`） | `claude_cross_*.json` |
| 4 | 同路/分歧合并 → 生成 `我的预测/latest.md` | 项目总监（`整合流水线.py`） | `latest.md` |
| 5 | 渲染 HTML（latest.html + 日期页） | `render_predictions.ps1` | `html/*.html` |
| 6 | 汇总页覆盖 sky 预测页 + 推送 | `整合流水线.py`（走 D盘 git，push 前汉化检查） | `data/output/predictions_*.html`（详细八维另存 `analysis_final_*.html`） |
| 7 | 次日复盘（四线计分 + 回写经验库） | sky4.0（`daily_run.py review`）+ 项目总监 | 复盘页 + 教训 |
| 8 | 每周一重训 + 交叉审查 | sky4.0 + sky三号 | 模型保鲜 |

**一条命令跑完全流程**：
```bash
python 整合流水线.py full --date 2026-09-08          # 全流程（含克劳德 DeepSeek 交叉）
python 整合流水线.py full --date 2026-09-08 --no-cross # 跳过克劳德（仅合并+渲染+推送）
```

---

## 四、同路 / 分歧判定规则（核心）

克劳德对每场输出四个要素：**方向 · 判定（同意/保留/反对）· 置信度 · 问题等级**。

### 4.1 方向归一化
把 sky4.0 的模型方向 / 看好方向 / 贝叶斯后验，与克劳德独立方向，都归一为 5 类：
`主胜 · 主不败 · 平局 · 客不败 · 客胜 · 跳过`

### 4.2 判定规则
| 克劳德判定 | 含义 | 项目总监合并动作 |
|---|---|---|
| **同意** | 与 sky 方向一致、无 P0 | 采用，标「同路」 |
| **保留** | 方向认同但 edge 薄/不确定 | 采用但降档，标「同路·保守」 |
| **反对** | 与 sky 方向相反 | **不跟**，标「分歧」 |
| P0（反向错误） | sky 方向与市场/模型全反向 | **强制不跟**，标「分歧·P0」 |
| P1（错标冷启动） | 冷启动标记失真 | 修正后重判 |
| P2（大小球分歧） | 仅大小球口径不一 | 方向照常，OU 标「分歧」 |

### 4.3 交叉要点（写入 latest.md）
- 共识场 = 双方方向一致 → 高置信
- P0 反向错误场 → 单独列出，明确「不跟」
- P1 错标冷启动场 → 列出失真点
- P2 大小球分歧场 → 列出「我判 X / sky 判 Y」

---

## 五、唯一预测契约

1. **每天只产出一份** `我的预测/latest.md`（+ 对应 html），董事长只参考这一份。
2. 覆盖范围：**五大联赛（英超/西甲/德甲/意甲/法甲）+ 欧冠**。其余联赛（英冠/荷甲/沙特等）默认不进「我的预测」，仅观察。
3. 每场必须含：**方向 · 首选比分 · 大小球 · 与 sky4.0 关系（同路/分歧）**。
4. 不构成投注建议；欧战/杯赛伤停首发按技能边界降权，不得编造。

---

## 六、每日时间表（北京时间）

| 时间 | 计划任务 | 动作 |
|---|---|---|
| 09:00 | 足球模型-每日预测 | sky 早盘（分析存档，不出预测） |
| 10:00 | 足球模型-每日复盘 | sky 复盘昨日 + 投注结算 |
| 18:00 | 足球模型-午盘快照 | sky 午盘（分析存档） |
| 21:00 | 足球模型-终盘预测 | sky 终盘（全天唯一出预测页） |
| **22:10** | **足球模型-整合流水线** | **克劳德交叉 → 合并 → 渲染 → 推送（无人值守，替换人工「跑」字）** |
| 周一 06:00 | 足球模型-每周重训 | 刷 CSV + 重训 + 回测 |

---

## 七、自动化与人工边界

- **无人值守**：22:10 计划任务自动跑 `整合流水线.py full`，克劳德用 DeepSeek 通道交叉，全程不需人点「跑」。
- **项目总监兜底**：每日 22:10 后本会话核对三条——(1) 终盘出了没 (2) 克劳德交叉跑了没 (3) latest.md 推送没。有 P0/P1 或新教训，立即人工复核 + 回写经验库。
- **重研增强**：五大联赛重要场次，项目总监可用 `research-football-match` 技能联网补强克劳德（伤停/首发/裁判），不编造、查不到降权。

---

## 八、关键数据路径

| 数据 | 路径 |
|---|---|
| sky 终盘预测（活） | `D:\足球大模型1.0\data\output\predictions_YYYY-MM-DD.json` |
| sky 八维笔记（活） | `D:\足球大模型1.0\data\output\analysis_notes_final_YYYY-MM-DD.json` |
| 克劳德交叉结果 | `E:\足球大模型\sky日记\data\output\claude_cross_YYYY-MM-DD.json` |
| 最终预测源 | `E:\足球大模型\我的预测\latest.md` |
| 整合汇总页 | `https://crazzy0924.github.io/football/data/output/predictions_YYYY-MM-DD.html` |
| 详细八维页 | `https://crazzy0924.github.io/football/data/output/analysis_final_YYYY-MM-DD.html` |
| 历史存档（旧） | `https://crazzy0924.github.io/football/my_preds/latest.html`（已停更，仅存档） |

---

## 九、纪律（继承 sky日记/CLAUDE.md，不降级）

- 时间自检：说「今天/明天/凌晨」前先查 `Get-Date`（北京时间）。
- 汉化检查：推送前 `pre_push_check.py` 不过不准 push。
- 冷启动永不投注、无信号不计入准确率、复盘幂等。
- 出现新教训 → 先回写 `sky日记/CLAUDE.md` 经验库 + `sky日记/工作自检表.md`，再改代码。

---

## 十、版本控制纪律（改文件怎么提交）

> **只认一个 canonical 仓库**：`D:\足球大模型1.0`（SSH 连 GitHub `crazzy0924/football.git`）。
> E 盘根目录和 `我的预测/` 不是 git 仓库；`E:\sky日记` 虽是 checkout 但脏且落后 origin，**一律不在 E 盘提交**。

### 10.1 文件归属与同步

整合文件分两处：**E 盘是运行版**（22:10 计划任务实际调用），**D 盘是提交版**（进 git）。两边内容必须一致。

| E 盘（运行版） | D 盘（提交版） |
|---|---|
| `E:\足球大模型\整合流水线.py` | `D:\足球大模型1.0\整合流水线.py` |
| `E:\足球大模型\整合流水线.cmd` | `D:\足球大模型1.0\整合流水线.cmd` |
| `E:\足球大模型\项目总监.md` | `D:\足球大模型1.0\项目总监.md` |
| `E:\足球大模型\sky日记\pipeline\claude_cross.py` | `D:\足球大模型1.0\pipeline\claude_cross.py` |
| `D:\足球大模型1.0\pipeline\team_names.py` | （本来就只在 D 盘，直接改） |

### 10.2 提交三步（死规矩）

```bash
# 1) 在 E 盘改完、测完 → 同步拷到 D 盘对应位置
Copy-Item "E:\足球大模型\整合流水线.py" "D:\足球大模型1.0\整合流水线.py" -Force
Copy-Item "E:\足球大模型\sky日记\pipeline\claude_cross.py" "D:\足球大模型1.0\pipeline\claude_cross.py" -Force
#    … 其余文件同理

# 2) 在 D 盘提交（汉化检查不过不准 push）
cd /d D:\足球大模型1.0
git add -A
python pre_push_check.py      # 不通过 → 修完重查 → 通过才 push
git commit -m "中文说明"

# 3) 推送，确认 push 成功才结束
git push origin master
```

### 10.3 铁律

- commit message 必须中文；`pre_push_check.py` 不过就 push = 违规。
- 只提交自己改的文件，不 `git add` E 盘无关的脏文件（E 盘 sky日记 的 data/state 等一律不碰）。
- 改完 E 盘忘了拷 D 盘 = 运行版和提交版分叉 → 下次提交前先比对两处是否一致。
